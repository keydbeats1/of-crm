{
  "name": "of-crm",
  "version": "1.0.0",
  "description": "OF CRM - OnlyFans Chat Operations Management System",
  "private": true,
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start",
    "lint": "cd web && npm run lint",
    "install:web": "cd web && npm install",
    "install:all": "npm install && npm run install:web",
    "dev:electron": "concurrently \"npm run dev --prefix web\" \"wait-on http://localhost:3001 && cd electron && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^7.2.0"
  },
  "dependencies": {}
}
